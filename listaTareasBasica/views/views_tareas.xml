<odoo>
  <data>
    <!-- explicit list view definition -->

    <record model="ir.ui.view" id="tareas.list">
      <field name="name">tareas list</field>
      <field name="model">tareas.tareas</field>
      <field name="arch" type="xml">
        <tree>
          <field name="tarea"/>
          <field name="prioridad"/>
          <field name="realizada"/>
        </tree>
      </field>
    </record>

    <!-- actions opening views on models -->
    <record model="ir.actions.act_window" id="tareas.action_window">
      <field name="name">Tareas</field>
      <field name="res_model">tareas.tareas</field>
      <field name="view_mode">tree,form,kanban</field>
    </record>


    <!-- server action to the one above -->
<!--
    <record model="ir.actions.server" id="lista_tareas.action_server">
      <field name="name">lista_tareas server</field>
      <field name="model_id" ref="model_lista_tareas_lista_tareas"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": model._name,
        }
      </field>
    </record>
-->

    <!-- Top menu item -->
    <menuitem name="Lista Tareas" id="tareas.menu_root"/>
    <!-- menu categories -->
    <menuitem name="Opciones lista tareas" id="tareas.menu_1" parent="tareas.menu_root"/>
    <!-- actions -->
    <menuitem name="Mostrar lista" id="tareas.menu_1_list" parent="tareas.menu_1" action="tareas.action_window"/>
  </data>
</odoo>